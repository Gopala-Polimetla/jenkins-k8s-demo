apiVersion: v1
kind: Service
metadata:
  name: jenkins-k8s-demo-service
  labels:
    app: jenkins-k8s-demo
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080
    protocol: TCP
  selector:
    app: jenkins-k8s-demo